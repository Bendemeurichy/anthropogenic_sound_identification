# =============================================================================
# HPC Requirements for Audio Separation Models - MINIMAL
# =============================================================================
# Stripped down to ONLY essential packages for training the 3 separation models
# 
# Size comparison (your local venv):
#   - TensorFlow:     1.8 GB  ❌ EXCLUDED (only for classifier, not trained on HPC)
#   - Torch:          1.7 GB  ✅ REQUIRED
#   - Transformers:    53 MB  ✅ REQUIRED (CLAPSep)
#   - Torchvision:     16 MB  ❌ EXCLUDED (not used)
#   - Plotly:          47 MB  ❌ EXCLUDED (visualization only)
#   - Faiss:           85 MB  ❌ EXCLUDED (not used in training)
#   - Jupyter/etc:     ?      ❌ EXCLUDED (notebook tools)
#   - BirdNet:         ?      ❌ EXCLUDED (separate tool)
#
# Expected venv size: ~5-6 GB (down from 11 GB)
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
torchaudio>=2.2.0

# WebDataset for efficient data loading
webdataset>=0.2.86

# Audio processing (core)
librosa>=0.10.0
soundfile>=0.12.0
resampy>=0.4.0

# Model-specific requirements
# CLAPSep
torchlibrosa>=0.1.0
pytorch-lightning>=2.0.0
laion-clap>=1.1.4
loralib>=0.1.2
transformers==4.30.2

# SudoRM-RF
thop>=0.1.1
progress>=1.6

# TUSS
einops>=0.6.0

# Training utilities (essential only)
tqdm>=4.65.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pyyaml>=6.0

# Monitoring (comment out if not using Comet.ml)
comet-ml>=3.30.0

# Utilities
psutil>=5.9.0
pytorch-warmup>=0.2.0
